[project]
name = "sage"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "questionary>=2.1.1",
    "rich>=15.0.0",
    "textual>=8.2.7",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
